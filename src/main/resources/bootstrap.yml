server:
  port: 3000
  
spring:
  application:
    name: BookingMS
  cloud:
    consul:
      host: localhost
      discovery:
        hostname: localhost
      config:
        fail-fast: false
        enabled: true
        prefix: config
        default-context: application
        data-key: data
        profile-separator: '::'
        format: YAML